"""
评估 Prompt 模块

供链路一的评估脚本 eval_docx.py 调用。模型读"word 提取的纯文本",
输出文档治理评估结果。

用法:
    from eval_prompt import PROMPT_TEMPLATE, JSON_SCHEMA

    prompt = PROMPT_TEMPLATE.replace("__DOCUMENT_TEXT__", plain_text)
    # 喂给模型,用 response_format + JSON_SCHEMA 约束输出
"""

PROMPT_TEMPLATE = """你是金融科技标准规范治理专家。请对下方标准文本进行评估,严格按指定 JSON 格式输出。

【输出要求】
- 输出纯 JSON,不要 markdown 代码块包裹,不要任何解释文字
- 字段名、枚举值严格按下方 schema,不要自创
- 所有字符串字段如无内容填空字符串 "",不要省略字段
- JSON 结构符号(逗号、冒号、引号、括号)必须使用英文半角

【关于输入文本中的占位符】

待评估文本是从 word 文档中机器提取的,其中以 `<` 开头、`>` 结尾的字符串是文本提取工具添加的**元素标记**,代表文档里真实存在但无法转为纯文本的元素。常见标记:

- `<图片>`:该段落是纯图片段,图片确实存在
- `<含图片>`:该段落既有文字也有图片
- `<空段>` / `<连续N个空段>`:空段落(word 里用于视觉分隔)
- `<表格: N行M列>`:该位置有表格,表格确实存在
- `<含图形对象>`:该段落含文本框、形状、SmartArt 等图形,图形确实存在
- `<含图形对象: "文本1", "文本2", ...>`:图形对象里提取出的文字,例如图注"图 1"

评估时重要规则:
1. **这些占位符代表的元素确实存在**,不要视为缺失
2. **不要将占位符本身视为"占位符残留"类问题**(硬约束 4)
3. **图片、表格、图形的内容无法被模型看到**,这是工具限制。评估基于文本和结构,不因视觉内容不可见而扣分或误触发"附件图片缺失"(硬约束 2)。"附件图片缺失"仅在连图注/表注文字也找不到时才算命中

# 一、元信息判断

## 1. 标准类型
- 原则性:通篇为方针、原则、目标性表述,不含具体执行步骤或量化要求
- 执行性:以具体步骤、量化指标、操作规程为主
- 混合性:原则与执行并存

## 2. 是否监管要求
- 是:文中明确提及响应某监管文件、上级要求、合规义务
- 疑似:文中无明确表述,但内容主题、措辞风格高度类似合规响应文档
- 否:明显为内部技术规范、操作指南,无合规背景信号

置信度三档:高 / 中 / 低。无明确文字依据时置信度不应给"高"。

## 3. 可检查性主导类型
看标准中要求"是否可被检查"的主导形态:
- 自动:要求多为可程序化判定(数值阈值、字段格式等)
- 半自动:要求需工具辅助但需人工确认
- 人工有标准:靠人工判断,但有明确判断依据
- 人工主观:靠人工经验主观判断

# 二、硬约束检查(命中=true,未命中=false)

命中时必须给原文证据片段(≤30字)。未命中时证据留空字符串。

1. 内部矛盾:同一份标准内,对同一对象/概念给出直接冲突的要求或定义。术语前后不一致、章节侧重点不同不算。
2. 附件图片缺失:正文出现"详见附件X""如图X所示""见表X"等指代,但在**整份文档的文本中(含占位符和图形对象提取文字)**搜不到对应的"附件 X"、"图 X"、"表 X"编号。占位符 `<图片>`、`<含图片>`、`<表格: ...>`、`<含图形对象>` 的存在表示该位置确实有对应元素,不要误判为"缺失"。只有当**连图注/表注的文字都找不到**时才算缺失。
3. 引用失效:引用的标准/法规明确已废止,或引用编号格式异常(如"GB/T XXXX""见第X章"等占位未填)。仅引用旧版本但未废止不算。
4. 占位符残留:文中出现 XXX、【待补充】、TODO、???、空方括号、明显的模板未填项。**本评估输入中以 `<` 开头、`>` 结尾的占位符(如 `<图片>`、`<空段>`、`<含图形对象: ...>`)是文本提取工具添加的元素标记,不属于此类"占位符残留"**,不要将其计入命中。
5. 歧义无法执行:关键执行条款表述歧义到两个理解相反的人都能从中读出依据,且无法通过上下文消解。

# 三、质量分(1-5 整数)

每项给"建议分"+"证据"(一句话说明判断依据,引用关键原文)。

## 1. 表述清晰度(全员必评)
- 5 = 术语定义齐全且一致,关键概念首次出现有界定,句式无歧义
- 4 = 个别术语未定义但行业通识可推断,整体清晰
- 3 = 存在多处指代不明或一词多义,需结合上下文反复确认
- 2 = 关键术语缺失定义,或同一概念用多种表述,影响理解
- 1 = 大量歧义表述,核心条款无法确定含义

## 2. 结构完整性(全员必评)
- 5 = 范围/术语/总则/具体要求/附则齐全,章节层级清晰,编号连续
- 4 = 主体结构完整,个别要素位置不当或编号小问题
- 3 = 缺少术语定义或适用范围等基础部分,但主体在
- 2 = 结构散乱,多个必要部分缺失,章节层级混乱
- 1 = 无明显结构组织,碎片化

## 3. 可操作性(仅执行性/混合性评,原则性必须为 null)
- 5 = 要求具体到可量化或可验证(数值阈值、明确步骤、检查方法),执行主体明确
- 4 = 主体要求可操作,少数条款较原则化
- 3 = 框架性要求为主,具体执行需另行细化
- 2 = 多用"应当""原则上""适当"等模糊措辞,缺少可验证标准
- 1 = 通篇原则表述,无任何可执行/可检查的具体要求

# 四、问题清单

汇总评估中发现的具体问题,最多 5 条,按严重度排序。每条结构化输出:
- 章节:问题所在章节号或位置(如"3.2节""第5章")
- 描述:问题简述(≤30字)
- 证据:原文片段(≤30字)

无问题时输出空数组 []。

# 五、输出 schema

{
  "标准名称": "",
  "元信息": {
    "标准类型": "",
    "是否监管要求": {
      "建议": "",
      "置信度": ""
    },
    "可检查性主导类型": ""
  },
  "硬约束": {
    "内部矛盾": {"命中": false, "证据": ""},
    "附件图片缺失": {"命中": false, "证据": ""},
    "引用失效": {"命中": false, "证据": ""},
    "占位符残留": {"命中": false, "证据": ""},
    "歧义无法执行": {"命中": false, "证据": ""}
  },
  "质量分": {
    "表述清晰度": {"建议分": 0, "证据": ""},
    "结构完整性": {"建议分": 0, "证据": ""},
    "可操作性": {"建议分": null, "证据": ""}
  },
  "问题清单": []
}

# 六、关键提醒(再次强调)
- 输出纯 JSON,无 markdown 包裹,无前后说明文字
- JSON 结构符号必须英文半角(逗号、冒号、引号、括号)
- 标准类型为"原则性"时,可操作性的"建议分"必须为 null
- 硬约束未命中时,证据为空字符串
- 问题清单最多 5 条
- 标准名称从文档首部提取,提取不到则用文件名

# 待评估标准文本

__DOCUMENT_TEXT__"""


JSON_SCHEMA = {
    "type": "object",
    "properties": {
        "标准名称": {"type": "string"},
        "元信息": {
            "type": "object",
            "properties": {
                "标准类型": {
                    "type": "string",
                    "enum": ["原则性", "执行性", "混合性"]
                },
                "是否监管要求": {
                    "type": "object",
                    "properties": {
                        "建议": {"type": "string", "enum": ["是", "疑似", "否"]},
                        "置信度": {"type": "string", "enum": ["高", "中", "低"]}
                    },
                    "required": ["建议", "置信度"],
                    "additionalProperties": False
                },
                "可检查性主导类型": {
                    "type": "string",
                    "enum": ["自动", "半自动", "人工有标准", "人工主观"]
                }
            },
            "required": ["标准类型", "是否监管要求", "可检查性主导类型"],
            "additionalProperties": False
        },
        "硬约束": {
            "type": "object",
            "properties": {
                name: {
                    "type": "object",
                    "properties": {
                        "命中": {"type": "boolean"},
                        "证据": {"type": "string"}
                    },
                    "required": ["命中", "证据"],
                    "additionalProperties": False
                }
                for name in ["内部矛盾", "附件图片缺失", "引用失效", "占位符残留", "歧义无法执行"]
            },
            "required": ["内部矛盾", "附件图片缺失", "引用失效", "占位符残留", "歧义无法执行"],
            "additionalProperties": False
        },
        "质量分": {
            "type": "object",
            "properties": {
                "表述清晰度": {
                    "type": "object",
                    "properties": {
                        "建议分": {"type": "integer", "minimum": 1, "maximum": 5},
                        "证据": {"type": "string"}
                    },
                    "required": ["建议分", "证据"],
                    "additionalProperties": False
                },
                "结构完整性": {
                    "type": "object",
                    "properties": {
                        "建议分": {"type": "integer", "minimum": 1, "maximum": 5},
                        "证据": {"type": "string"}
                    },
                    "required": ["建议分", "证据"],
                    "additionalProperties": False
                },
                "可操作性": {
                    "type": "object",
                    "properties": {
                        "建议分": {
                            "type": ["integer", "null"],
                            "minimum": 1,
                            "maximum": 5
                        },
                        "证据": {"type": "string"}
                    },
                    "required": ["建议分", "证据"],
                    "additionalProperties": False
                }
            },
            "required": ["表述清晰度", "结构完整性", "可操作性"],
            "additionalProperties": False
        },
        "问题清单": {
            "type": "array",
            "maxItems": 5,
            "items": {
                "type": "object",
                "properties": {
                    "章节": {"type": "string"},
                    "描述": {"type": "string"},
                    "证据": {"type": "string"}
                },
                "required": ["章节", "描述", "证据"],
                "additionalProperties": False
            }
        }
    },
    "required": ["标准名称", "元信息", "硬约束", "质量分", "问题清单"],
    "additionalProperties": False
}
