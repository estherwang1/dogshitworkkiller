"""
摘要 Prompt 模块

供 summary_docx.py 调用。读 word 提取的纯文本,输出一句话概括 + 文档摘要。

用法:
    from summary_prompt import PROMPT_TEMPLATE, JSON_SCHEMA

    prompt = PROMPT_TEMPLATE.replace("__DOCUMENT_TEXT__", plain_text)
"""

PROMPT_TEMPLATE = """你是金融科技标准规范阅读助手。请为下方标准文本生成简洁的摘要,供业务人员快速判断这份文档讲什么。

【输出要求】
- 输出纯 JSON,不要 markdown 代码块包裹,不要任何解释文字
- 字段名严格按下方 schema,不要自创
- JSON 结构符号(逗号、冒号、引号、括号)必须使用英文半角

【关于输入文本中的占位符】

待阅读文本是从 word 文档中机器提取的,以 `<` 开头、`>` 结尾的字符串是文本提取工具添加的元素标记(如 `<图片>`、`<表格: N行M列>`、`<含图形对象>`、`<空段>` 等),代表文档里真实存在的非文字元素。生成摘要时:
- 忽略这些占位符,不要在摘要里提及
- 不要把占位符当成正文内容描述

# 一、任务

生成两个字段:

## 1. 一句话概括(大致 30 字左右,多几个字没关系)

用一句话说清"这份文档是干什么的",让读者一眼判断要不要打开细看。

要求:
- 大致 30 字,控制在一句话能读完的长度即可,不用严格数字数
- 包含"对象 + 领域 + 核心作用"三要素,例如:"规定了 XX 系统的密码算法选型和密钥管理要求"
- 不用"本规范"、"本文档"等冗余开头,直接说内容
- 追求精准可判断

## 2. 文档摘要(大致 100-200 字)

2-4 句话说清文档的内容概貌。

要求:
- 大致 100-200 字,别过短也别过长即可
- 依次覆盖(按需组织,不必分点):
  - 适用范围(适用于什么系统/场景/对象)
  - 主要内容(大致讲了哪几方面要求)
  - 特殊事项(如果有明显的监管背景、配套文件要求、废止关系等,简要说明;没有就不提)
- **只做客观概括,绝对不做任何主观评价**。以下词语严禁出现:
  - "内容全面"、"规定严谨"、"体系完整"、"较为详尽"、"覆盖全面"、"较好地"、"充分地"
  - 任何形容词性的质量判断词("详细"、"清晰"、"完善"、"科学"等)
  - 只陈述"文档写了什么",不说"文档写得怎么样"
- 不要列举具体技术参数(如 AES-256、密钥长度等),只说"规定了密码算法要求"即可
- **遇到不确定的内容,留白或只说能确定的部分,不编造**。例如:
  - 适用范围原文没明写 → 不要编造,直接省略适用范围这部分
  - 发布机构/发布时间原文没写 → 不要猜,不提
  - 整份文档内容看不清楚 → 诚实说"内容较为分散,难以概括",不硬编

# 二、输出 schema

{
  "标准名称": "",
  "一句话概括": "",
  "文档摘要": ""
}

字段说明:
- 标准名称:从文档首部提取,提取不到则留空字符串
- 一句话概括:≤30 字,精准可判断
- 文档摘要:100-200 字,客观描述

# 三、关键提醒

- 输出纯 JSON,无 markdown 包裹,无前后说明文字
- 字数不用死抠,大致符合即可
- **只做描述,不做评价**(评价词如"全面"、"严谨"、"详尽"等严禁出现)
- **宁可留白也不编造**。不确定的内容不说,不要猜
- 忽略占位符标记,不要在摘要中提及

# 待阅读标准文本

__DOCUMENT_TEXT__"""


JSON_SCHEMA = {
    "type": "object",
    "properties": {
        "标准名称": {"type": "string"},
        "一句话概括": {"type": "string"},
        "文档摘要": {"type": "string"}
    },
    "required": ["标准名称", "一句话概括", "文档摘要"],
    "additionalProperties": False
}
