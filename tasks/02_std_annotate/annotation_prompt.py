"""
分块识别 Prompt 模块

供链路二的标注脚本调用。模型读"带编号和格式信号的段落文本",
返回哪些段落应该标为切块边界。

用法:
    from annotation_prompt import PROMPT_TEMPLATE, JSON_SCHEMA

    prompt = PROMPT_TEMPLATE.replace("__DOCUMENT_TEXT__", formatted_text)
    # 喂给模型,用 response_format + JSON_SCHEMA 约束输出
"""

PROMPT_TEMPLATE = """你是文档结构分析专家。下方是一份标准规范的段落列表,每段带唯一编号和(可能的)格式信号。请识别出所有应作为"切块边界"的段落,用于后续知识库 RAG 的分块处理。

【输出要求】
- 输出纯 JSON,不要 markdown 代码块包裹,不要任何解释文字
- 字段名、枚举值严格按下方 schema,不要自创
- JSON 结构符号(逗号、冒号、引号、括号)必须使用英文半角

# 一、输入格式说明

每行是一个段落,格式为:
    [段落编号] 段落文本 | 格式信号

格式信号可能包含(不存在的信号不会出现):
- style=XXX:段落样式名。带 "(目录/图注)" 后缀的是目录项或图注,不是正文标题
- 加粗:整段加粗
- 字号=N:显式设置的字号

特殊占位符:
- <含图片>:段落同时含文字和图片(通常是图注)
- <图片>:段落仅含图片无文字
- <空段>:空段落
- <连续N个空段>:连续多个空段的折叠显示
- <表格: N行M列>:表格
- <含图形对象>:段落含文本框、形状、SmartArt 等图形对象
- <含图形对象: "文本1", "文本2", ...>:图形对象里提取出的文字(形态多样,可能是图注、流程图节点名等)

**重要**:这些占位符都代表文档里真实存在的元素,是工具添加的标记,不是正文。识别节标题时:
- 占位符本身不会是节标题
- 含占位符标记的段落,判断依据仍看段落的文字部分(占位符前/后的正文)

# 二、识别目标

切块边界是指:**适合作为 RAG chunk 起始位置的结构性段落**,通常是文档的章/节/条标题。

不要纠结"章、节、条"的抽象命名,目标是切出**大小合理、语义完整**的 chunk。一般规则:
- 如果文档有三级层级(如 1 / 1.1 / 1.1.1),优先选倒数第二级(1.1)作为切块边界
- 如果只有两级,选顶层(1)
- 如果只有一级,就选那一级
- 整份文档的切块粒度应尽量一致,避免有的 chunk 特别长有的特别短

# 三、判断规则

## 应标为切块边界的情况(正例)

1. **编号规整的标题**:段落以明显的层级编号开头,如 `1`、`1.1`、`3.2.1`、`第一章`、`第三节`、`一`、`二`等
2. **有标题样式信号**:style 以 Heading、Title、标题、章标题、节标题、一级标题、二级标题等开头
3. **手工伪标题**:无标题 style,也无明显层级编号,但符合以下特征组合才标记:
   - 整段加粗
   - 字数很少(通常 ≤ 20 字)
   - 语气短促、概括性强(像标题而非叙述)
   - 字号明显大于上下文段落(有这个信号更可靠,但不是必要条件)
   
   只有同时满足"加粗 + 短 + 标题语气"才标记。单独加粗但文字长的叙述段不标,如"**本章规定了密码算法的使用要求,包括算法选型、密钥管理、加密模式等内容**"——这是加粗正文不是标题。

4. **段落内嵌的编号标题(重要,易漏识别)**:段落以规整的层级编号开头(如 `6.1.1`、`3.2.1`、`第三节`、`第五条`),即使整段内容很长、无标题 style、无加粗,也**必须标记**为切块边界。
   - 关键识别特征:**编号位于段落最开头**(第一个字符就是编号)
   - 这类段落的形态通常是"编号 + 简短标题 + 一大段正文内容",例如 `6.1.1 生物特征识别信息收集 本节规定了...`
   - 这是标准规范里非常常见的写法,大部分 X.Y.Z 级的条款都是这种形态,请务必识别
   - 不要因为"段落长"就误判为正文引用——区分标准见负例 3

## 不应标为切块边界的情况(负例)

1. **目录项**:style 带 "(目录/图注)" 后缀,直接排除
2. **图注/表注**:style=Caption 或类似,或文字以"图 X-X"、"表 X-X"开头
3. **正文里引用其他条款**:如"根据 3.2 条规定"、"见第二章第三节"、"应符合本规范 6.1.1 条的要求"——这是正文中的引用,不是结构性标题。
   - **关键区分**:编号是否在段落最开头。
   - 编号在段落开头(第一个字符就是编号)→ 是段落内嵌标题(正例 4),**应标记**
   - 编号在句子中间(前面还有其他文字)→ 是正文引用,**不标记**
   - 示例对比:
     - `6.1.1 生物特征识别信息收集 本节规定了...` → 开头就是 `6.1.1`,标记
     - `密钥管理应符合本规范 6.1.1 条的要求` → `6.1.1` 在句子中间,不标记
4. **表格、图片占位**:<表格: ...>、<图片>、<含图片>、<空段> 不标
5. **列表项**:编号为 (1)(2)(3) 或 a) b) c) 的列表项不算切块边界(过细)

## 粒度原则

整份文档的切块粒度应以一种主要层级为主(如都按 1.1 级切分),但允许合理差异:
- 总则、范围、术语等概括性章节可以用更粗的粒度(如按 1 级切)
- 技术细节部分可以用更细的粒度(如按 1.1.1 级切)
- 判断依据:切出的 chunk 应尽量**语义完整且大小适中**,避免极端不平衡(如大部分 chunk 只有几行,突然出现一个几千字的 chunk)
- 相同层级在不同位置不要差别对待(比如 2.1 标为边界,就不要只标 3.1 不标 3.2)

## 粒度下限警告

**如果文档有明显的 X.Y.Z 三级编号(如 6.1.1、6.1.2),不要只切到 X.Y 级就停**。段落内嵌的 X.Y.Z 级标题(见正例 4)必须识别出来,否则切块会过粗,单个 chunk 内容过多,影响 RAG 召回精度。

自检方式:识别完成后,对照文档实际出现的最深层级编号。如果文档里能看到多个 X.Y.Z 级编号段落(如 6.1.1、6.1.2、6.2.1 等),但你的标记列表里只有 X.Y 级甚至 X 级标记,说明漏识别了段落内嵌标题,应重新检查。

# 四、判断依据分类

每个被标记的段落,必须归入以下四类之一:

- **编号规整**:靠编号格式识别出来的(如 `3.2 密码算法` 这种)
- **格式突出**:靠 style/加粗/字号识别出来的
- **语义判断**:既没有规整编号也没有格式信号,纯靠上下文和文字语义判断
- **混合依据**:同时有编号和格式信号

# 五、置信度规则

- **高**:同时有标题样式(style=Heading 或类似)和规整编号
- **中**:只有一类强信号(只有 style,或只有编号,或只有加粗+字号)
- **低**:纯语义判断,或信号矛盾(如 style 是正文但加粗又像标题)

无明确依据时不给"高"。

# 六、输出 schema

{
  "标准名称": "",
  "节标记列表": [
    {
      "段落编号": "P0023",
      "节标题预览": "3.2 密码算法要求",
      "识别依据": "混合依据",
      "置信度": "高"
    }
  ],
  "统计": {
    "总段落数": 0,
    "标记段落数": 0,
    "低置信度数": 0
  }
}

字段说明:
- 标准名称:从文档首部提取,提取不到则留空字符串
- 段落编号:必须与输入里的编号完全一致(如 "P0023"),不要改动
- 节标题预览:抄录段落原文前 30 字以内,不要编造、不要改写
- 识别依据:四个枚举值之一
- 置信度:三档之一
- 统计.总段落数:输入的段落总数(含空段/表格/图片,折叠的连续空段按实际数量算)
- 统计.标记段落数:节标记列表的长度
- 统计.低置信度数:置信度为"低"的数量

# 七、few-shot 示例

## 示例 1:编号规整 + Heading 样式(高置信度,混合依据)
输入行: [P0045] 3.2 密码算法要求 | style=Heading 2, 加粗
判断: 标记。依据=混合依据,置信度=高

## 示例 2:纯中文编号(高置信度,混合依据)
输入行: [P0012] 第三章 密码管理 | style=Heading 1, 加粗, 字号=16
判断: 标记。依据=混合依据,置信度=高

## 示例 3:手工伪标题(中置信度,格式突出)
输入行: [P0078] 密码算法的选用 | 加粗
判断: 标记。依据=格式突出,置信度=中
说明:无标题 style 也无编号,但加粗 + 6 个字 + 标题语气,符合手工伪标题特征

## 示例 4:目录项(不标)
输入行: [P0008] 3.2 密码算法要求	15 | style=TOC 2 (目录/图注)
判断: 不标记
说明:style 带"(目录/图注)"后缀,明确是目录项,直接排除

## 示例 5:段落内嵌的编号标题(重要,高置信度,编号规整)
输入行: [P0234] 6.1.1 生物特征识别信息收集 本节规定了生物特征识别信息收集过程中应遵循的原则和方法,包括收集前的告知义务、收集时的最小化原则、以及收集后的存储要求等内容...
判断: 标记。依据=编号规整,置信度=高
说明:段落以 `6.1.1` 开头(第一个字符就是编号),后面跟简短标题和大段正文。这是段落内嵌标题的典型形态,必须标记。不要因为"段落长"就跳过。

## 示例 6:正文里引用其他条款(不标,与示例 5 对比)
输入行: [P0512] 密钥管理应符合本规范 6.1.1 条的要求,同时参照第二章相关规定执行
判断: 不标记
说明:`6.1.1` 出现在句子中间(前面有"密钥管理应符合本规范"),这是正文引用不是标题。区分示例 5 和示例 6 的关键:编号是否在段落最开头。

# 八、关键提醒

- 输出纯 JSON,无 markdown 包裹,无前后说明
- 段落编号必须照抄输入,不要改格式或补零
- 节标题预览必须是原文摘抄,最多 30 字,不要编造
- 识别依据只能是四个枚举值之一
- 置信度只能是三档之一
- 整份文档切块粒度要一致,不要忽粗忽细

# 待识别文本

__DOCUMENT_TEXT__"""


JSON_SCHEMA = {
    "type": "object",
    "properties": {
        "标准名称": {"type": "string"},
        "节标记列表": {
            "type": "array",
            "items": {
                "type": "object",
                "properties": {
                    "段落编号": {"type": "string"},
                    "节标题预览": {"type": "string"},
                    "识别依据": {
                        "type": "string",
                        "enum": ["编号规整", "格式突出", "语义判断", "混合依据"]
                    },
                    "置信度": {
                        "type": "string",
                        "enum": ["高", "中", "低"]
                    }
                },
                "required": ["段落编号", "节标题预览", "识别依据", "置信度"],
                "additionalProperties": False
            }
        },
        "统计": {
            "type": "object",
            "properties": {
                "总段落数": {"type": "integer"},
                "标记段落数": {"type": "integer"},
                "低置信度数": {"type": "integer"}
            },
            "required": ["总段落数", "标记段落数", "低置信度数"],
            "additionalProperties": False
        }
    },
    "required": ["标准名称", "节标记列表", "统计"],
    "additionalProperties": False
}
